## 2026-05-10 Git初期設定・Gemini CLI連携ログ

### 実施内容
- Gemini CLI上で `git --version` を実行し、`2.54.0.windows.1` を確認。
- Gitユーザー情報を設定。
  - `git config --global user.name "yukiko-archi"`
  - `git config --global user.email "inakamiso@gmail.com"`
- リポジトリを初期化済みであることを確認し、初回コミットを作成。
- ネットワークドライブ上で発生した `dubious ownership` エラーに対応。
  - `git config --global --add safe.directory "G:/"` を設定。
- `git add .` -> `git commit -m "Initial commit"` -> `git status` を実行し、`working tree clean` を確認。
- GitHubリモートを設定して初回pushを完了。
  - `git remote add origin https://github.com/yukiko-archi/yukikos-Obsidian-File.git`
  - `git push -u origin main`

### 結果
- ローカルGit管理: 完了
- 初回コミット: 完了
- GitHub連携（`origin/main` 追跡設定）: 完了

### 次回以降の基本運用
```bash
git add .
git commit -m "変更内容"
git push
```

## 本日のAIコスト最適化・作業ログ
実施日： 2026年5月11日
担当： 阿部 ＆ Gemini

1. 現状分析と「犯人」の特定
分析： 直近のAPI利用額の内訳を確認。

判明： Gemini 2.5 Flash への入力トークンが 約1,170万トークン（¥561分） と突出しており、これが高額請求の主因であることを特定。

エラー確認： 5月10日に 223回の「TooManyRequests (429)」エラー が発生していたことを確認。短時間の連続リクエストやループが原因と推測。

2. インフラ設定の更新（Windows環境）
環境変数の追加： Windowsのシステム設定から、新しい変数 GEMINI_MODEL を作成し、値を gemini-3.1-flash-lite に設定。

目的： CLIやツールがデフォルトで「2.5」ではなく、最安の「3.1 Lite」を優先して選ぶように誘導。

3. Gemini CLI の内部設定変更
モデルの切り替え： CLI上で /model コマンドを実行。

永続化の設定： Tabキー を使用して Remember model for future sessions: true に変更し、設定を保存。

設定ファイルの直接更新： CLIからの提案を受け入れ、.gemini/gemini.md および settings.json の中身を、最安モデルである gemini-3.1-flash-lite-preview に書き換え。

自動Pro切り替えの無効化： コスト高を招く「自動でProモデルに切り替わる機能」をオフにし、徹底した節約モードへ移行。

4. 安全運用のためのガードレール設置
ループ検知の有効化： 「A potential loop was detected」の警告に対し、Keep loop detection enabled を選択。

目的： プログラムの暴走による意図しない課金を、AI側で自動ストップできる状態を維持。

📝 阿部さんへの申し送り事項
現在の状態： 「最安モデル固定」かつ「安全装置（ループ検知）作動中」の、最強の節約フォーメーションです。

今後の挙動： 回答内容がシンプルすぎると感じた時だけ、手動でモデルを上げればOKです。

反映待ち： 今日の作業分は、明日以降のグラフで「Gemini 3.1 Flash-Lite」として安く計上されるはずです。